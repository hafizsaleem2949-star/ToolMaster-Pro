# ToolMaster Pro

Fresh React + Vite multi-tool website.

Includes 100+ tool entries, categories, search, Student AI Helper UI, Text to Video UI and an admin dashboard foundation.

Run: `npm install` then `npm run dev`

Build: `npm run build`

Real AI answers/video rendering require a secure server-side API; never expose API keys in client-side code.
